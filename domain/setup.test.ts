import { describe, expect, it } from "vitest";

// Placeholder that proves the test setup runs, locally and in CI.
// Delete it when the first real domain test is added.
describe("test setup", () => {
  it("runs", () => {
    expect(1 + 1).toBe(2);
  });
});
